# 手動QA: 記録の保存（I-008 / I-009 相当）

**目的**: 記録シートで **曲名 →（画面上は）スコア → Intent → メモ → 保存** が一貫して動くかを確認する。  
（実装の並びは `RecordingSheetContentView` に準拠）

## 前提

- アプリをビルドしてシミュレータまたは実機で起動する。
- **選曲**タブを開き、右上 **「記録を追加」** でシートを開く。

---

## 必須（Must）

1. **手動で曲名**を入力する（例: `テスト曲A`）。
2. **スコア**を変更する（Slider、0〜100）。
3. **Intent** を1つ選ぶ（Shout / Emo / Practice）。
4. **メモ**を任意で入力する（空でも可）。
5. **「保存する」**をタップする。

**期待結果**

- インラインエラーが出ない。
- 保存中はオーバーレイ＋`ProgressView` が出て、**保存中はボタンが押せない**（I-010）。
- 成功後、**シートが閉じる**。
- **履歴**タブに自動で切り替わる（`RootView` の `selectedTab = .history`）。
- **履歴画面の一覧表示**（直近の記録が見えること）は **I-014** の範囲。現状 `HistoryRootView` がプレースホルダーの場合は、**タブ切替まで**を必須の合格とし、データ確認は SwiftData デバッグ／I-014 後に行う。

---

## 任意（保存失敗と再試行）

ローカル SwiftData のみの保存では、通常は失敗しにくい。**意図的に失敗を再現する**場合の例:

- **プレビュー / テスト用**: `PreviewSessionRepository` などで `save` が `throw` するように一時変更してビルドする。
- **デバッガ**: `SwiftDataSessionRepository.save` の `try modelContext.save()` の直前にブレークし、例外を投げる／`save` をスキップするなど（上級者向け）。

**再試行時に見たいこと（I-011 は別タスクだが挙動の目安）**

- エラー表示＋**再試行**で、保存処理が走ること。
- **保存中は** `isSaving` により **連打・二重タップで同時に2回 `save()` が走らない**こと（UI層 I-010）。
- **注意**: データ層の **冪等性（I-011）** は未完了の場合、`exists(uuid)` による抑止がないため、**失敗後の再試行は「新しい `SingingSession` を別 UUID で保存し直す」**動きになる。  
  「同じ1回分の保存がDBに2行増えないか」を見る場合は、**履歴の件数**と **同じ曲の singCount** を確認する（部分失敗パターンは I-011 実装後に再確認推奨）。

---

## 結果メモ（テンプレ）

| 項目 | 結果 |
|------|------|
| 日付 | |
| 環境 | Simulator / 実機（OS バージョン） |
| 必須: 保存〜シート閉じ〜履歴タブ | OK / NG |
| 任意: 失敗再現 | 実施した / スキップ |
| 備考 | |
